##  3-Bit Down Counter (Binary/Grey Mode)

This Proteus simulation demonstrates a 3-bit **down counter** that operates in two modes:

- **Binary Counter** (when input = `0`)
- **Gray Code Counter** (when input = `1`)
